default namespace = "tag:textalign.net,2015:ns"

include "incl/TAN-LM-core.rnc" {
    ## TAN-LM files point only to one source
    source-list = lang-outside
    pointer-to-div-range = empty
    pointer-to-div-item = empty
    seq-pos-ref = empty
}